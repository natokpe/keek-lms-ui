import $        from "jquery";
import LazyLoad from "vanilla-lazyload";

$(document).ready(function() {
    "use strict";

    var lazyLoadInstance = new LazyLoad({
      // custom settings
    });
});
